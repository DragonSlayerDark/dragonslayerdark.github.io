import { CardPrefixPipe } from './card-prefix.pipe';

describe('CardPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new CardPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
